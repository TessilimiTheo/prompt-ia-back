version: "3.8"

services:
  app:
    build:
      target: node_development
      dockerfile: Dockerfile
      context: .
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - EXEC_ENV=development